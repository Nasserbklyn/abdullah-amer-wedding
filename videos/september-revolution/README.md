# فيديو ثورة 26 سبتمبر (20 ثانية)

موشن-جرافيك بدقة 1920×1080 / 30 إطاراً في الثانية عن ثورة 26 سبتمبر 1962 ومعارك الجيش الوطني اليمني اليوم.

| ملف | الوصف |
| --- | --- |
| `yemen-september-revolution.mp4` | الفيديو النهائي الأفقي 16:9 (H.264 + AAC) |
| `yemen-september-revolution-9x16.mp4` | النسخة العمودية 9:16 (1080×1920) للستوري والريلز |
| `composition.html` | التصميم والحركة (HTML/CSS/JS). افتحه في المتصفح لمعاينة حيّة متكررة |
| `render.js` | يصوّر كل إطار عبر Playwright ويمرّره إلى ffmpeg |
| `music.py` | يولّد الموسيقى التصويرية (طبول وإيقاع سينمائي) بـ numpy إلى `music.wav` |
| `fonts/` | خطوط Cairo و Amiri (رخصة SIL OFL) |

## المشاهد

| الزمن | المشهد |
| --- | --- |
| 0 – 4 ث | العلم يملأ الشاشة ثم ينكمش إلى شارة، عنوان «ثورة 26 سبتمبر 1962 — فجر الجمهورية اليمنية» |
| 4 – 8.5 ث | «ثورة صنعت الجمهورية» وأبرز أهدافها |
| 8.5 – 15.5 ث | «من ثورة سبتمبر إلى معارك استعادة الجمهورية»، الجبهات (مأرب، الجوف، تعز، الضالع، حجة، شبوة)، ختم «انتصارات» |
| 15.5 – 20 ث | علم متموّج، «النصر للجمهورية — 26 سبتمبر ثورة لا تموت — اليمن أولاً» |

## إعادة الإخراج

```bash
python3 music.py                       # يحتاج numpy
FFMPEG=/path/to/ffmpeg node render.js             # النسخة الأفقية 1920×1080
FFMPEG=/path/to/ffmpeg node render.js --portrait  # النسخة العمودية 1080×1920
```

لتغيير النصوص عدّل `composition.html` مباشرة؛ التوقيتات كلها داخل الدالة `render(t)`. للمعاينة العمودية افتح `composition.html?portrait=1`.

---

## ريلز فيسبوك — «سبتمبر شهر اليمنيين المقدّس» (30 ثانية)

النسخة المخصّصة لفيسبوك ريلز: 1080×1920، 30 ثانية، رسوم أصلية بالكامل مع خريطة اليمن
الحقيقية ووقائع سبتمبر 2026 مؤرّخة ومنسوبة. **اقرأ `PUBLISHING.md`** — فيه الوصف
المقترح، مصادر كل واقعة، وسبب عدم استخدام لقطات إخبارية.

| ملف | الوصف |
| --- | --- |
| `yemen-september-reel-30s.mp4` | الريلز النهائي بالأهزوجة (H.264 High@4.0 + AAC) |
| `yemen-september-reel-30s-silent.mp4` | نفس الريلز بلا صوت — لإضافة أغنية مرخّصة من مكتبة الموسيقى داخل تطبيق فيسبوك |
| `reel-30s.html` | تصميم وحركة الريلز — افتحه في المتصفح لمعاينة حيّة |
| `render-reel.js` | يصوّر الإطارات عبر Playwright ويمرّرها إلى ffmpeg |
| `zamil-30s.py` | يولّد الأهزوجة الأصلية `zamil-30s.wav` (إيقاع زامل + تصفيق + هتاف جماعي) |
| `music-30s.py` | الموسيقى السينمائية البديلة `music-30s.wav` (غير مستخدمة في النسخة الحالية) |
| `thumbnail.html` | تصميم الغلاف |
| `thumbnail-9x16.png` | غلاف الريلز 1080×1920 |
| `thumbnail-4x5.png` | غلاف منشور الفيد 1080×1350 |
| `yemen-map.json` | مسارات خريطة اليمن مسقطة إلى بكسلات التركيب |
| `yemen.geo.json` | حدود اليمن من Natural Earth (ملك عام) |
| `PUBLISHING.md` | دليل النشر: المواصفات، المصادر، الوصف، معايير Meta |

### مشاهد الريلز

| الزمن | المشهد |
| --- | --- |
| 0 – 3.8 ث | العلم يملأ الشاشة ثم ينكمش إلى شارة — «سبتمبر: شهر اليمنيين المقدّس» |
| 3.8 – 8.5 ث | 26 سبتمبر 1962 — يومَ قامت الجمهورية اليمنية |
| 8.8 – 11.4 ث | «وفي سبتمبر 2026… الميدان يتكلّم من جديد» |
| 11.5 – 26 ث | خريطة اليمن + أربع وقائع مؤرّخة: حيس، غارات تعز والحديدة، اللبنات/الجوف، الكدحة |
| 26 – 30 ث | علم متموّج — «سبتمبر شهر اليمنيين المقدّس · النصر للجمهورية» |

### إعادة الإخراج

```bash
python3 zamil-30s.py                                              # الأهزوجة
FFMPEG=/path/to/ffmpeg MUSIC=zamil-30s.wav node render-reel.js    # الريلز 1080×1920
```

لتحديث الوقائع عدّل مصفوفة `FACTS` في `reel-30s.html` فقط — البطاقات والعلامات على
الخريطة تُبنى منها تلقائياً. ولإضافة موقع جديد أضِف إحداثياته في `yemen-map.json`.
